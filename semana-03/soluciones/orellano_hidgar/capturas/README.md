# Capturas requeridas

Agregar aqui las evidencias visuales si el docente las solicita en el aula virtual:

1. DevTools mostrando cookie `session` con `HttpOnly` y `SameSite=Lax`.
2. Acceso denegado 403 con `usuario@test.com` en `/reportes`.
3. Acceso permitido 200 con `supervisor@test.com` en `/reportes`.
4. Acceso denegado 403 con `usuario@test.com` en `/admin/panel`.
5. Acceso permitido 200 con `admin@test.com` en `/admin/panel`.
6. Demo `/demo/fixation` antes y despues del login.

Las pruebas funcionales ya fueron ejecutadas y documentadas en `laboratorio/respuestas.md`.
